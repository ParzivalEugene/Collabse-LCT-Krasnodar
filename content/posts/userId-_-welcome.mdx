---
title: Знакомство с компанией
description: ПИСЯ ПОПА ПОПА ПИСЯ
---

На этом слайде ты видишь какой же ебать сочный у нас дизайн и mdx парсер

## Пример блока кода

```tsx
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}

export function formatDate(input: string | number): string {
  const date = new Date(input)
  return date.toLocaleDateString("en-US", {
    month: "long",
    day: "numeric",
    year: "numeric",
  })
}

export function absoluteUrl(path: string) {
  return `${env.NEXT_PUBLIC_APP_URL}${path}`
}
```

Не ну ты видел этот сочный стафф бро. Ладно брат теперь давай пробежимся по фичам

> Здарова, я тут типо оставил коммент, и ты так же можешь чел

## Фичи епта

<div className="grid sm:grid-cols-2 gap-4 mt-6">

<Card href="/docs/documentation">

### Вот такие лютые карточки

Заценил этот хайп

</Card>

<Card href="/docs/marketing" disabled>

### Marketing

The marketing site with landing pages.

</Card>

<Card href="/docs/app" disabled>

### App

The dashboard with auth and subscriptions.

</Card>

<Card href="/docs/blog" disabled>

### Blog

The blog built using Contentlayer and MDX.

</Card>

</div>

