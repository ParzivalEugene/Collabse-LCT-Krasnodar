export interface UserModel {
  id: string;
  username: string;
  role: string;
}

export interface Employee {
  workerId: string;
  username: string;
  name: string;
  surname: string;
  jobTitle: string;
}
